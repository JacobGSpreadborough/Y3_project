# Run with `dart run ffigen --config ffigen.yaml`.
name: DemoLibraryBindings
description: |
  Bindings for `src/demo_library.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/demo_library_bindings_generated.dart'
headers:
  entry-points:
    - 'src/demo_library.h'
  include-directives:
    - 'src/demo_library.h'
comments:
  style: any
  length: full
